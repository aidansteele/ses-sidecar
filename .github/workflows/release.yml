name: release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-go@v2
        with:
          go-version: 1.24
          cache: true

      - uses: ko-build/setup-ko@v0.7

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and publish container image with ko
        run: |
          export KO_DOCKER_REPO=ghcr.io/$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          ko build --platform=all --bare --sbom=none --tags ${{ github.ref_name }},latest ./cmd/ses-sidecar

      - name: Build binaries for release
        run: |
          mkdir -p dist
          GOOS=linux GOARCH=amd64 go build -ldflags="-s -w" -o dist/ses-sidecar-linux-amd64 ./cmd/ses-sidecar
          GOOS=linux GOARCH=arm64 go build -ldflags="-s -w" -o dist/ses-sidecar-linux-arm64 ./cmd/ses-sidecar
          GOOS=darwin GOARCH=amd64 go build -ldflags="-s -w" -o dist/ses-sidecar-darwin-amd64 ./cmd/ses-sidecar
          GOOS=darwin GOARCH=arm64 go build -ldflags="-s -w" -o dist/ses-sidecar-darwin-arm64 ./cmd/ses-sidecar
          GOOS=windows GOARCH=amd64 go build -ldflags="-s -w" -o dist/ses-sidecar-windows-amd64.exe ./cmd/ses-sidecar

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          generate_release_notes: true
          files: dist/*
          body: |
            ## 🐳 Container Image
            
            ```bash
            docker pull ghcr.io/quentinbtd/ses-sidecar:${{ github.ref_name }}
            ```
            
            ## 📦 Binaries
            
            Download the appropriate binary for your platform from the assets below.
